#include <stdio.h>

void zerar(int vet[], int tamanho) {
    int k;
    for (k=0;k<tamanho;k++)
    {
        vet[k] = 0;
    }
}

void imprimir(int vet[], int tamanho) {
    int k;
    for (k=0;k<tamanho;k++)
    {
        printf("%d", vet[k]);
    }

}

void push(int vet[], int num) {
    printf("hahah");

}

void main() {
    int linha = 3;
    int coluna = 3;
    int tamanho = linha*coluna;

    //vetor que simula a matriz
    int vet[tamanho];

    zerar(vet);
}